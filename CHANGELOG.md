# CHANGELOG

Kenesis Loop Kitのすべての変更はこのファイルに記録されます。
フォーマットは [Keep a Changelog](https://keepachangelog.com/ja/1.0.0/) に準拠し、
バージョン管理は [Semantic Versioning](https://semver.org/lang/ja/) に従います。

---

## [Unreleased]

<!-- KLK-090: KLK-061〜095 の34件は、受け取った人が読む文書として**主題ごとにまとめて**記録する。
     1件ずつ並べると内部の経緯が主になり、「このツールで何ができるか」が読み取れなくなるため。 -->

### Fixed（KLK-135・配布パッケージに別案件の設計書が入っていた）

- `tools/make-package.sh` が `docs/` をディレクトリごと写していたため、
  **このシステムとは別の案件の設計書**が配布物に混ざっていた。
  組み立て時に `docs/designs/` を allowlist（`KLK-*.md` / `README.md` / `_TEMPLATE.md`）で絞るようにした
- リポジトリ本体からは消していない。外すのは配布物だけ
- 「名指しで除く」ではなく「残すものを列挙する」形にしたので、今後どんなファイルが増えても漏れない。
  `check_klk135` が、**実際に別案件の設計書を置いてから組んで**配布物に出ないことを確かめる

### Fixed（KLK-132・Windows で 起動.bat が動かなかった）

2026-09-16 の実使用レビュー（Windows 実機）で報告された「ダブルクリックしても無反応」の修正。

- `起動.bat` を **CP932(Shift_JIS) + CRLF** で保存するようにした。
  これまでは macOS で書かれたままの形（改行 LF のみ・文字コード UTF-8）で、
  日本語 Windows の `cmd.exe` が行を正しく読めず、**Windows ユーザー全員が起動できなかった**
  （`'ode' is not recognized` / `@echo off` が効かない、という症状もこれが原因）
- `chcp 65001` の行は削除した。読み取りのずれは `chcp` が実行される前に起きているため効いていない
- BOM 付き UTF-8 は実機で**悪化**したので採らない（BOM が先頭行に食い込み `@echo off` すら効かなくなる）
- `.gitattributes` で Git に改行を触らせないようにした（LF へ潰されると元に戻ってしまう）
- 処理の流れとメッセージは変えていない。ダメ文字（CP932 の2バイト目が `\` になる文字）1件だけ言い換えた
- `check_klk132` が、文字コード・改行・BOM・骨格に加えて
  **実際に組んだ配布物とバイト一致すること**まで見張る

### Added（KLK-125・SCR-001 の型選択にも日本語ラベル）

KLK-124（型の図解アイコン）の第1段。

- 生成設定の「ページ構成 → 行ごとの設定 → レイアウト型」も
  **「カード群＋名称＋価格（pat-cards）」**のように日本語ラベル付きで出るようになった。
  KLK-117 は比較画面にしか入っておらず、**2箇所のうち片方が英語のまま**だった
- 送る値（生成指示書へ渡る `type`）は**マーカーのまま**。「自動（案ごとに振り分け）」も残る
- ラベル表が bridge.py（正）と一字一句一致することを `check_klk125` が照合し、
  実ブラウザで描かれた選択肢を `e2e_klk125` が確かめる
- ★あわせて **e2e ハーネスの取り違えを修正**。`Page.navigate` の直後は前の文書
  （about:blank）がまだ現在の文書で readyState が 'complete' なので、負荷で遷移が遅れると
  **まだ真っ白なページを読んで**いた（フルスイートで KLK-116 が1件落ちた）。
  目的の URL へ移り終わったことまで確かめるようにし、回帰テストで固定した

### Added（KLK-123・カタログ登録時に画像を自動で軽くする）

理恵さんの要望。KLK-110 で「167枚 773MB → 217MB」と後からまとめて縮めた作業を、
取り込み時に済ませて二度とやらずに済むようにした。

- 登録が確定した画像を**横幅1600px上限・PNG→JPEG**で自動的に軽くする。
  縮め方は `tools/shrink-catalog-images.py` の `shrink_one` をそのまま使う
- ★**失敗しても登録は止まらない**（`sips` が無い環境・壊れた画像でも登録は成功する）。
  画像が大きいことは、登録できないことより軽い問題だから
- ★**原本は残らない**（軽くした画像で置き換わる）。規約とスキルに明記した
- 対象は**これから取り込む分**だけ。既存の画像は変わらない
  （実測: `catalog/img/` は原本のまま 810MB・1600px超が21枚。
   KLK-110 が縮めていたのは配布パッケージ内のコピーだった）

### Fixed（KLK-122・短いラベルの窮屈な折り返し）

KLK-118 で「はみ出してはいないが窮屈な箇所は検出できない」と申し送った宿題の消化。

- `tools/verify-responsive.node.js` に判定 F（短いラベルの窮屈な折り返し）を追加。
  **8文字以内のラベルが1行6文字未満の幅で2行以上に折れている**状態を捕まえる。
  ★箱の高さ÷行高では測れない（ボタンの上下余白で2行に見える）ので、
  `Range` で**文字が実際に何行に折れたか**を数える
- **見本03 を修正**: 768px でナビが 40px まで潰れ「アクセ／ス」と折れていた（3案とも）。
  FLOW の「メンテナン／ス」も同様。ナビ項目を折らない指定＋641〜900px で間隔と余白を詰めた
- ★別件も修正: 見本03 の FLOW で**見出しと説明文が同じ行に流れていた**
  （`.t` が inline のままで `margin` だけ指定され `display:block` が抜けていた）。
  幅に関係なく起きていた
- 規約に §8.2（短いラベルを折らない）・§8.3（見出しと説明文を行で分ける）を追加
- 妨害注入を2種: わざと狭めると検出できること／**余白を足しただけでは誤検出しないこと**
- ★あわせて **e2e ハーネスの固まりを修正**。Chrome を操作する WebSocket の接続待ちに
  上限が無く、開かなければ永久に待つ作りだった（KLK-116/117/120 の3本すべて）。
  単独なら5秒で終わる e2e が、入れ子でスイートを回すと300秒のタイムアウトで落ちていた。
  20秒の上限と error/close の検知を入れ、同じ作りに戻らないよう検査で固定した

### Fixed（KLK-121・編集したタグがカタログ一覧に反映されない）

理恵さんが KLK-120 の編集機能を実際に使って見つけた不具合。
「主配色を変えても、カタログ一覧のタグが編集前のまま」。原因は2つ重なっていた。

- **① タグが導出値に追随していなかった。** カードは `tags` を表示しているが、
  タグは取り込み時のまま作り直されなかった。テイスト・主配色・カラム構成を直すと
  **その分だけタグを入れ替える**ようにした。手で書いたタグ（業種の略称・自由語）は残る。
  無関係な項目だけ直したときはタグを変えない
- **② ★保存後に一覧を描き直していなかった（KLK-120 の実装ミス）。**
  `loadCatalog()` は取得して返すだけで画面には反映しない契約なのに、結果を捨てていた。
  サーバが計算した値が画面へ届いていなかった
- 業種は導出値に含めない。実データでは略称でタグに入っており（53件中21件しか一致しない）、
  推測で消すと手で書いたタグを壊すため
- 検収を追加: `check_klk120.py` に G群9項目、`test_palette_klk120.py` に9テスト、
  `e2e_klk120.node.js` に E9（**カードの表示そのもの**が入れ替わることを実ブラウザで確認）

### Added（KLK-120・登録済みカタログエントリの編集）

理恵さんの要望「登録後のラフの情報を編集したい（背景トーンを自分で付けたい）」への対応。
これまでは取り込み時にしかタグを直せず、登録後は削除して入れ直すか `catalog.json` の
手編集しかなかった（手編集は書き間違えると画面がカタログを丸ごと空として扱う）。

- **実績カタログの各カードに ✏ ボタン**を追加。モーダルでタイトル・業種・テイスト・
  カラム構成・メモ・主配色・背景トーン・種別を編集できる
- **変えた項目だけを送る。** 触っていない項目（検索用 `tags`・`sectionLayouts` 等）は
  巻き添えで消えない。「（未設定）」を選べる項目は、選ぶとキーごと消える（元に戻せる）
- **`id` / ファイル名 / 登録日時は変更できない**（身元と来歴）。送られてきたら拒否する
- 保存は削除（KLK-068）と同じ安全手順: Origin 検証 → 入力検証 → 実在確認 →
  **更新後の全体を検証（不正なら1件も書き換えない）** → 原子的置換。画像には触らない
- 検収 `check_klk120.py`（36項目）＋ `test_palette_klk120.py`（12テスト）＋
  `e2e_klk120.node.js`（8項目・実ブリッジと実ブラウザ）。
  **実効果テストはサンドボックスで動く**ので、社外秘の実カタログには触れない

### Added（KLK-119・背景トーンを主配色と別の2軸目として足す）

実ユーザーのフィードバック「主配色にモノトーンはあるがホワイト・ブラックが無い。
背景で白や黒を使いつつモノトーンでない配色のラフが選びにくい」への対応。

- 実績カタログに **背景トーン `bgTone`（`ライト` / `ダーク`）** を**任意項目**として追加。
  SCR-004 に絞り込み行と、取り込み承認フォームの選択（「（未設定）」も選べる）を用意した
- **主配色（16種）は触っていない。** 主配色は配色ジェネレーターの色相ファミリーであり、
  白黒は色相ではなく背景の明暗なので、同じ軸に混ぜず**2軸目**にした
  （例: 主配色=ブルー × 背景トーン=ダーク で絞り込める）
- **既存167件は未設定のまま妥当**（additive）。付与は `/catalog-import` と同じ流儀で別途行う
- 副産物の修正: カタログ検証が**ハッシュ不能な値（配列など）で例外になっていた**。
  手書きの catalog.json ひとつでブリッジが落ちる状態だったので、`bgTone` と既存の `colors` の
  両方を「型が違えば語彙外として弾く」形へ直した（KLK-119 のテストで発見）
- 検収 `check_klk119.py`（23項目）＋ `test_palette_klk119.py`（13テスト）:
  縦串の語彙一致・additive・主配色を汚していないこと・妨害注入

### Fixed（KLK-118・768px のレイアウト崩れ）

実ユーザーのフィードバック「768px で背景から要素がはみ出す／横並びが画面外へ出る」
「アタリの比率が 3:4(4:3) から大きく逸脱して細長い」への対応。

- **`tools/verify-responsive.node.js` を新設**。生成物を**実際に描いて測る**検査。
  横スクロール／親からのはみ出し／箱からのあふれ／アタリの実測比率／横並びの重なり を見る。
  既存の検査は CSS の宣言を読むので「4/3 と書いてあるのに細長い」を捕まえられなかった
- 116ファイル・**全84型**を走査。実際の生成物で崩れていたのは **2型**だけだった
  （フィクスチャの逸脱は全て比率規定 KLK-072 より前の golden で、当時は違反ではない）
- **見本03（クリニック）を修正**: 768px で画面に横スクロールが出ていた。
  原因は ①列の多い表が器に包まれていない ②**地図が自分の列を超えて隣の情報パネルに重なっていた**
  （`align-items:stretch` で高さが伸び `aspect-ratio` が幅を押し広げた）
- 規約に **§3.0.2 実際に描いたときの許容帯**（数値は実測から決定）と
  **§3.0.3 列数の多い表は幅に関係なく横スクロールできる器に入れる** を追加。
  `pat-mosaic` に「1タイルを1行の高さのまま3列以上にまたがせない」歯止めを追加
- 検収 `test_palette_klk118.py`（10テスト）: 見本の回帰防止＋**妨害注入**
  （わざと崩して検出できること・4:3 と 3:4 を誤検知しないこと・16/9 は通さないこと）

### Changed（KLK-117・型の選び直しに日本語ラベルを出す）

実ユーザーのフィードバック「型名が英語のみだとどのような型なのかが掴めない」への対応。

- 比較画面の **🔄 セクション再生成** の型セレクタが、
  **「価格表／料金プラン（price-table）」** のように日本語ラベルつきで出るようになった（全 **84型**）
- ラベルは**考えて付けたものではなく、生成規約 §12.1.2／§12.1.3 のその型の説明文からの抜き書き**。
  `check_klk117` が、ラベルを区切りで割った語の**すべて**が規約の説明文に実在することを照合する
  （言い換えて作れば落ちる）
- 送る `desiredType` は従来どおりマーカーのまま。ラベルを返さない旧ブリッジでも壊れない
- 型セレクタの文字色を本文色へ（白地に 3.68:1 → 12.00:1。ラベルが意味を担うようになったため）
- 検収チェック 3層: `check_klk117.py`（21項目）／`test_palette_klk117.py`（12テスト・言い換え検知の妨害注入）／
  `e2e_klk117.node.js`（テンプレート→make_compare→実ブリッジ→ヘッドレス Chrome で**実際の選択肢の文字**を確認。
  labels を剥がした応答を食わせる妨害注入つき）

### Added（KLK-116・配色ジェネレーターの配色をモック生成画面へ自動で渡す）

実ユーザーのフィードバック「CSS変数をコピー → 貼り付けの手間が気になる」への対応。

- 配色ジェネレーター（`palette/index.html`）の各案に **「モック生成画面へ送る」** ボタンを追加。
  押すと、開いている生成設定画面（SCR-001）の配色欄に**その場で自動で入る**
- 生成設定画面を**後から**開いた場合は、黙って色を変えずに「配色が届いています［反映する］」を表示し、
  押したときだけ入る（開いただけで配色が変わらない）
- 受け渡しは同一オリジン（ローカルブリッジ配信）での `BroadcastChannel`＋`localStorage`。
  受け取る側は schema／version と各色（`normalizeHex`）を検証し、壊れた値は捨てる。
  従来の「貼り付け → 取り込み」はそのまま使える（送れない環境ではその案内を出す）
- 検収チェック 3層: `check_klk116.py`（静的・両画面の定数値を照合）／`smoke_klk116.node.js`（純粋関数・妨害注入）／
  `e2e_klk116.node.js`（ブリッジ実起動＋ヘッドレス Chrome で**別タブへ実際に届く**ことを確認。Chrome が無ければ明示 SKIP）

<!-- DADS:BEGIN -->
### Changed（KLK-115・配布パッケージからデザインシステムを外す）

- **モック生成システムの配布パッケージに DADS を含めないようにした。** 別案件のため
  （2026-09-09 の方針決定・2026-09-10 に「リポジトリは分割せず当面このまま」と確定）
- `tools/make-package.sh` に **`--with-design-system`（既定OFF）** を追加。
  既定ビルドからは `docs/design-system/` と `デザインシステムの使い方.html` が外れる（約2.7MB / 付けると約3.8MB）
- ファイルを外すだけでは `CLAUDE.md`・`agents/`×3・`README.md`・`CHANGELOG.md` に
  **参照先の無い記述**が残るため、`<!-- DADS:BEGIN/END -->` で囲んだ区間を
  `tools/strip-dads-sections.py` が組み立て時に取り除く。**リポジトリ本体からは消していない**
- 検収チェック `tests/site/check_klk115.py`＋`tests/test_palette_klk115.py`（12項目 / 11テスト）:
  既定ビルドに DADS 語が1つも無いこと、同梱が固定 allowlist と一致すること、
  **リポジトリ直下へ実際にフォルダを置いても配布物に現れないこと**（別案件の混入防止）を実ビルドで確認
- 既存の検査は**消さずに付け替えた**。`check_klk111` D2 と `check_klk112` G1/G2 は
  `--with-design-system` を付けたビルドで検証する（「付ければ入る」側も見て空検査を防ぐ）

<!-- DADS:END -->
<!-- DADS:BEGIN -->
### Added（KLK-111・デジタル庁デザインシステムの導入）

**デザインの基準を公的な標準に合わせる**
- **`docs/design-system/`** にデジタル庁デザインシステム（DADS）β版 v2.17.1 のドキュメント（Markdown）一式を同梱（125ファイル / UIコンポーネント49種 / 基本デザイン8種 / アクセシビリティガイドライン）。本文は未編集
- `docs/design-system/_REFERENCE_GUIDE.md`: 参照ルール（全件読み込み禁止・`MANIFEST.md` 起点・目的別の参照先・49種のパス一覧・「DADSに含まれないもの」の明示）
- `docs/design-system/_ATTRIBUTION.md`: 出典・ライセンス条件・バージョン・成果物への出典記載場所・更新手順
- CLAUDE.md にセクション「デザインシステム参照ルール（DADS）」を追加。ドキュメント管理表・ポリシー管理の原則テーブルにも転記
- `agents/architect.md` / `implementer.md` / `reviewer.md` にDADSの参照ルール・遵守基準・レビュー観点を追加
- 検収チェック `tests/site/check_klk111.py`＋`tests/test_palette_klk111.py`: 生成規約側（`DRAFT_RULES` / `WIREFRAME_RULES` / `draft-gen/` / `palette/`）へのDADS参照の混入防止、49種・MANIFEST・出典の整全性、パッケージ実ビルドへの同梱を機械的に検査

### Changed（KLK-113・デザインシステムとモック生成の分離）

- 2026-09-09 の方針決定により、デジタル庁デザインシステム（DADS）とモック生成システムは
  **別案件**として進める。`デザインシステムの使い方.html` からモック生成への言及をすべて除去
  （「なぜキットに入っているのか」「モック生成とは別物です」節と対比図・FAQ1件・別冊案内）
- 代わりに「どんなときに使うのか」節と、「125ファイル → MANIFEST → 必要な1〜5ファイル」の図を追加
- `check_klk112.py` に D7（モック生成へ言及していない）を追加。妨害注入テストで再混入を検知できることを検証
- **未決**: モック生成のパッケージから DADS を外す件。`docs/` 丸ごと同梱のため
  `docs/design-system/` が入るが、CLAUDE.md と agents×3 が DADS を参照しているため、
  DADSだけ除くと参照先の無い記述が残る。リポジトリ分割の判断とセットで扱う

### Added（KLK-112・デザインシステムの使い方マニュアル）

- **`デザインシステムの使い方.html`**: DADSの引き方と守り方を初心者向けにまとめた単一HTML
  （章立てサイドナビ・CSS/SVG図解・外部依存ゼロ）。「色の値はDADSに含まれない」「モック生成とは
  別軸で混ぜない」「出典表記は利用条件」の3点を軸に、Claude Code への頼み方の例まで掲載
- フォーカスインジケーター（黄＋黒の2重構造）を実際に <kbd>Tab</kbd> で体験できる見本を同梱。
  見本の色はマニュアル側で決めた値である旨とコントラスト実測値（8.77:1）を明記
- 検収チェック `tests/site/check_klk112.py`＋`tests/test_palette_klk112.py`: 本文の数字
  （49種・ファイル数・コントラスト比）を実データと再計算で照合、外部リソース依存ゼロ、
  リンク切れなし、モック生成画面へ載せていないこと、パッケージ実ビルドへの同梱を検査
- README・配布パッケージの同梱リストに追加

### Changed（KLK-111）
- README.md: 「開発ループを回す人へ」の表にデザインシステムの行、ライセンス節に同梱第三者コンテンツの出典を追加

> 既存の生成規約（`WIREFRAME_RULES.md` / `DRAFT_RULES.md`）と `palette/` はDADS導入の対象外とし、
> 変更していない。配色の正は従来どおり各規約側にある。
<!-- DADS:END -->

### Added（KLK-061〜106・2026-09-02〜09-08）

**受け取ってすぐ使える形に（KLK-105/106）**
- **`起動.command` / `起動.bat` をフォルダ最上位へ**。開いてすぐダブルクリックできる
- **`はじめにお読みください.txt`** を同梱。必要なもの・起動手順・つまずいたときをまとめた
- `起動.command` に**専用アイコン**（macOS のみ。Windows のバッチはアイコンを持てません）

**使い方マニュアル（KLK-098）**
- **`使い方マニュアル.html`** を同梱。ダブルクリックで開く単一HTML（外部依存ゼロ）
- サイドナビ付きの章立て。画面ごとの操作・レイアウト型14種×6型の一覧・図解6枚
- README とは役割分担（README＝置く/起動/困ったとき、マニュアル＝画面ごとの操作）
- 生成画面の見出し脇に **📖 使い方マニュアル** のリンク（別タブで開く・KLK-099）

**ページ構成を自由に組める（KLK-086〜089・091〜094）**
- 「④ 本文コンテンツ構成」がチェックボックスから**ページ構成リスト**へ。載せるセクションを
  **上から順に並べ**、**同じセクションを複数**置ける（例: MENU を「ランチ」「ディナー」で2つ）
- 並べ替えは **`⠿` を掴んでドラッグ**、または ↑↓ ボタン
- 行ごとの「設定」で**見出し・リード文・レイアウト型・詳細ページ誘導ボタン**を指定できる。
  型を選ぶと3案ともその型になり、選ばなければ案ごとに振り分ける
- 並び順を指定すると**3案とも同じ並び**になる（案の違いは配色とレイアウト型で出る）
- 置ける数は本文あわせて12個・同じセクションは3個まで（ACCESS/CONTACT/SEARCH は1個）
- 入力例をセクションごとに用意（PRICE なら「料金プラン」、VOICE なら「お客様の声」）
- 参考サムネイルは既定18件で畳み、「さらに表示する」で開く（**選択済みは畳んでも隠れない**）

**生成後にセクションを作り直せる・型を入れ替えられる（KLK-078〜080・092）**
- 比較画面の 🔄 で、**そのページに実際にあるセクション**を選んで作り直せる
  （以前は6種の固定リストで、実ページと食い違っていた）
- **レイアウト型を選び直せる**（例: ギャラリーを均等グリッド→ベントー型へ）
- 作り直したあと、ブリッジが**実ファイルを読み直して**指示どおりになったか・規約を守っているかを
  機械的に確かめ、守られていなければ画面に赤字で伝える
- **1案だけ作ったときも比較画面が開く**。表示幅の切替も 🔄 も3案と同じように使える

**見本サイトURLから配色を読み取れる（KLK-083）**
- URL を貼って「配色を読み取る」で、よく使われている色をスウォッチで確認し、配色欄へ反映できる。
  AI を通さない決定的な処理で、外部アクセスは**社内ネットワークを覗けないよう厳重に制限**している

**実績カタログを画面から管理できる（KLK-063〜068）**
- カタログ画面へ**ドラッグ&ドロップ**で画像を追加 → AI がタグ付けを提案 → **画面で確認・修正してから登録**
- カードの 🗑 で登録を取り消せる（画像は消さず `catalog/.trash/` へ退避）
- 主配色をムードカラー ジェネレーター準拠の**16種**へ拡張

**配布とドキュメント（KLK-069〜071・077）**
- `README.md` を全面作成（「できないこと」を先に書く構成）・`tools/make-package.sh` で配布フォルダを組み立て
- **Windows 対応**（`起動.bat`・hook の両OS化）
- 見本となる生成ページ3点を `samples/` に同梱
- **カタログ同梱版（A）/ 同梱なし版（B）** の2種類を作れる。A では README が同梱前提の案内へ自動で切り替わる

**比較画面（KLK-062）**
- 画面幅プレビュー切替（全幅 / 768px / 375px）。スマホ版の二重生成は廃止

### Changed（KLK-061〜110）
- **カタログ同梱のパッケージが軽くなった**（KLK-110）。約775MB → **約220MB**（72%減）。
  配布用に画像の横幅を1600pxまでに収め、PNG を JPEG へ入れ替えます。
  **お手元の元画像はそのまま**で、パッケージを作るときだけ変換します
- **比較画面（`compare.html`）を固定テンプレートから書き出すようにした**（KLK-103）。
  以前は生成のたびに作り直していたため、幅切替・🔄 セクション再生成・型セレクタといった
  機能が**作り直しのたびに落ちることがあった**。テンプレート化により、どの生成物でも
  同じ機能が確実に付く。案件ごとに変わるのは配色だけ。生成する量が減るので少し速くなる
- 生成規約に**セクション内型プール**を整備し、14セクション×各6型から案ごとに振り分けるように
  （KLK-051〜058。NEWS/PRICE/FAQ/ACCESS/CONTACT/SEARCH/SNS ほか）
- 画像アタリの比率を **4/3** に統一。狭いカラムではカード内を縦積みに畳む（KLK-072・073）
- 業種・テイストの語彙を実績カタログ側の正へ一本化（KLK-059）
- ブリッジのタイムアウトを **900→1800秒**（実測 262〜847秒に対し余裕が6%しかなかった・KLK-095）

### Fixed（KLK-061〜100）
- 生成画面の**使い方マニュアルへのリンクが開けなかった**不具合（KLK-100）
- 「もっと探す → 実績カタログを開く」の**リンクが開けなかった**不具合（KLK-100）
- 実績カタログが**同じタブで開き、入力途中の設定が失われていた**問題（KLK-101・別タブへ）
- 実績カタログの**取り込みが複数枚で失敗する**不具合（KLK-107）。1回に処理する枚数を
  3枚までに絞り、残りは次回へ回すようにした（画面に「残り○枚」と出ます）
- **同じ画像を二重に取り込み待ちへ入れてしまう**問題（KLK-107・内容で重複を検出）
- すでに起動しているのに起動しなおすと、**英語のエラーが大量に出て落ちる**問題（KLK-107）。
  「すでに起動しています」と案内するようにし、画面のタブも増えないようにした
- **3案を指定したのに2案しかできていないのに「完了しました」と報告していた**不具合（KLK-104）。
  案の本数で成否を判定するようにし、足りないときは画面にそう出る。
  `verify-mockup` も指示書の案数と実際を突き合わせる
- 画面に出ていた**開発用の番号**（REQ-010 / KLK-022 / SCR-001 等）を非表示に（KLK-085）。
  利用者には意味の分からない記号だったため。HTMLコメントとして残るので開発側は従来どおり追える
- 生成物の比較画面のボタン名に**内部の画面名が混入**していた問題（KLK-085・見本と mockups 計5件）
- MV の「SCROLL ↓」がボタンと**重なる**不具合（KLK-097）。誘導は**画面左端に縦組み**になり、
  MV は誘導用の帯を左右に予約するので、見出し・リード文・ボタンをすべて置いても重ならない。
  あわせて MV の縦幅が**ファーストビューを覆う**高さになった（`full`/`split`/`band`/`center-scroll`）
- カタログ取り込みが**一度も登録に到達していなかった**不具合（非対話実行と人間確認ゲートの衝突・KLK-064/065）
- 🔄 セクション再生成が `<section>` を使うページで**全番地404**になっていた不具合
  （`<div>` 決め打ち・見本の2/3で機能していなかった・KLK-078）
- HERO `panel-band` のパネルが 1200〜1280px で**段落ち**していた不具合（KLK-081）
- 同じ端末なのに **Origin 判定で403**になる不具合（文字列一致で `http://[::1]:…` を弾いていた・KLK-084）
- UI と実装の食い違いを是正（「対応予定」表示・見本での 🔄 の見え方など・KLK-061・081）

### Removed
- 配布物に無関係な成果物をリポジトリから除去（KLK-060・パッケージ化準備）: `site/`（KLK-002 のコーポレートサイト静的サンプル）・`flyers/`（別件のチラシHTML 2点）・`src/`（`.gitkeep` のみの未使用ディレクトリ）。あわせて対象を失った `tests/site/check_klk002.py` を除去。**Git履歴は保持**しており `git checkout {削除前のコミット} -- site` で復元できる。デザインラフ・ジェネレーターのサンプルは、パッケージ化時に見本となる生成ページ3〜5点をあらためて生成して同梱する方針（`mockups/` の外）。

### Added
- 配色ジェネレーター `palette/index.html` v1.2（KLK-005）: CSS変数コピーに「HEXコード一覧」形式を追加し切替可能に（`hexListOf`／`copyTextOf`・グローバル `copyFormat` 保持・微調整後の値を反映）／KLK-005 検証 `tests/site/check_klk005.py`（静的S1-S13）・`tests/site/smoke_klk005.node.js`（動的D1-D7）・`tests/test_palette_klk005.py`（unittestラッパー）
- 配色ジェネレーター `palette/index.html` v1.1（KLK-004）: メインカラーの傾向に「ゴールド」「シルバー」を追加（色味サブ選択6変種・相性パートナーによる3案生成・最終段クランプ `clampMetalBand`・金属風グラデーションの参考表示）／CSS変数コピー（`--color-main` 等4変数）／URL共有（選択状態＋シードをURLパラメータ化・`replaceState` 同期）／生成ボタンを上段横長に分離
- KLK-004 検証: `tests/site/check_klk004.py`（静的S1-S12）・`tests/site/smoke_klk004.node.js`（動的D1-D6）・`tests/test_palette_klk004.py`（unittestラッパー、node無し環境はskip）
- 配色ジェネレーター `palette/index.html` v1.0（KLK-003）: 言葉・スライダーから配色3案を生成するツール。同系色モード・トーンチップ（PCCS風9種）・WCAGコントラスト比バッジ・色覚多様性チェック・70:25:5比率バー＋サイト風モック・H/S/L微調整モーダル等
- トップページFV刷新の静的サンプル `site/`（KLK-002）と設計書・検証テスト

### Changed
- 配色ジェネレーターの可読性バッジをWCAG等級から日常語＋信号色へ変更（KLK-005・読みやすい◎/小さい文字は注意△/読みにくい✕、注意・NG時に直し方を提示、生数値と正式等級はツールチップへ集約。判定ロジックは不変）
- 配色ジェネレーターの「ジャンル（業種）」を単一選択（ラジオ＋指定なし）に変更（KLK-004・複数業種の色相平均による濁りを解消）

## [1.2.0] - 2026-07-05

### Added
- スキル `spec-interview`（`.claude/skills/spec-interview/`）: 非エンジニア向けに対話形式で要件定義書 `docs/SPEC.md` を作成・改訂する。最短/フルの2モード、標準技術構成の「反証がなければ適用」（`DEFAULT_STACK.md`）、SPECテンプレート（`SPEC_TEMPLATE.md`）を同梱
- スキル `wireframe-gen`（`.claude/skills/wireframe-gen/`）: SPECの画面一覧（セクション6）から単一ファイル静的HTMLのワイヤーフレーム・ハブページ（index.html）・PC/スマホ比較ビュー（compare.html）を生成する。生成規約 `WIREFRAME_RULES.md` を同梱（外部依存ゼロ・中忠実度・コントラスト規律・Mermaid遷移図のCSSフォールバック）
- CLAUDE.md にスキルを登録: スラッシュコマンド一覧・開発ループフロー図の前工程（SPEC作成→ワイヤーフレーム→チケット作成）・ドキュメント管理表・investigatorのSPEC不在時の誘導
- `.gitignore` に `*.skill`（スキルのZIP配布物を除外し、ソースの正を `.md` に一本化）
- `docs/wireframes/`（出力ディレクトリのプレースホルダ `.gitkeep`）

### Removed
- 静的テンプレート `docs/SPEC.md`（要件定義は `/spec-interview` から生成する方式へ移行。テンプレートはスキル内 `SPEC_TEMPLATE.md` が正）

---

## [1.1.0] - 2026-06-17

### Added
- 状態検証hook: `validate_ticket_state.py`（PreToolUse）/ `check_loop_integrity.py`（Stop）/ `_ticket_lib.py`。チケット状態機械を機械的に強制
- ループ観測性: `record_metrics.py`（PostToolUse）でステータス遷移を `tickets/.metrics.jsonl` に記録、`.claude/metrics/aggregate.py` で集計、`/metrics` コマンドで表示
- `.claude/settings.json` にhook登録（PreToolUse / PostToolUse / Stop）
- 自動テスト `tests/`（unittest・依存なし）: 検証ルール・各hook・メトリクス集計を66ケースでカバー
- リトライカウンタ強制の二層化: L2 カウンタ単調性（PreToolUse・減少禁止でcap回避を防止）/ L3 差し戻し履歴照合（Stop・`.metrics.jsonl` の差し戻し回数とカウンタを和で照合し、増やし忘れ＝上限不発火の穴を塞ぐ）
- チケットフロントマターに `retry_counts` を追加し、本文「リトライカウンタ」セクションと併用（ハイブリッド管理）
- CLAUDE.md「ポリシー管理の原則」転記表・状態検証hook節
- 権限モード `auto` の既定化（`.claude/settings.json` `permissions.defaultMode = "auto"`）。コマンド承認でループが頻繁に停止する問題を解消
- ループ起動時チェック「Autoモードの確認」（`start-loop.md` 起動時チェックリスト / `orchestrator.md` Responsibilities）と、ポリシー管理表への該当行追記
- CI: `.github/workflows/test.yml`（push(main/develop)・PRで `tests/` を Python 3.10–3.12 マトリクス実行、stdlibのみ）
- `docs/batch-loop.md`: 複数チケットの連続ループ実行ガイド（事前バッチ承認で各完了時の承認ゲートを跨ぐ運用）
- `.claude/settings.json` の allow に `sed -n`（読み取り専用）のみ再追加（probe/テスト後始末の複合コマンド向け）。`echo` はファイル書き込みベクタ（`echo > file`）を再び開くため再追加せず、auto モードの分類器に委ねる。環境固有の `.venv/bin/python` / `rm -f` ルールは `settings.local.json` へ分離

### Changed
- 設計書を単一 `docs/DESIGN.md` から `docs/designs/{ID}.md`（チケット単位分割）へ変更
- 設計書frontmatterから `status`（draft/approved/superseded）を撤廃。設計の進行はチケットstatusを唯一の真実とし、履歴はGitに委ねる（dead state解消）
- orchestratorのステータス定義をCLAUDE.md準拠に統一（独自ステータス `review_approved` / `review_rejected` を廃止）
- architect後の人間承認ゲートを廃止し、実装ループを完全自律化
- README: Obsidianが任意であることを明言

### Removed
- `docs/DESIGN.md`（`docs/designs/` へ移行）
- `.claude/settings.json` の allow から `awk` / `echo`（ファイル書き込みをWrite権限に一元化）。※ `sed -n`（読み取り専用）のみ probe/テスト用途で再追加（上記 Added 参照）

---

## [1.0.0] - 2026-06-14

### Added
- エージェント定義: orchestrator / investigator / architect / implementer / tester / reviewer
- チケットテンプレート: ticket.md / ticket-bug.md
- スラッシュコマンド: /start-loop / /new-ticket / /improvement-loop / /archive
- ドキュメント: SPEC.md / docs/designs/ (チケット単位設計書) / obsidian-setup.md テンプレート
- チケットダッシュボード: tickets/_index.md
- Claude Code設定: .claude/settings.json（権限設定）
- .gitignoreテンプレート（プライベート用・パブリック用）
- 2ループ構造（実装ループ・改善ループ）の導入
- git-flowブランチ戦略の採用
- Gitコミット規約（チケットIDプレフィックス）

---

<!--
## バージョニングの指針

MAJOR（X.0.0）: エージェント定義の大幅な変更・ループフローの変更・既存プロジェクトとの後方互換性が失われる変更
MINOR（0.X.0）: 新規エージェント追加・新規コマンド追加・テンプレートの追加
PATCH（0.0.X）: ドキュメントの修正・既存テンプレートの軽微な修正・バグ修正

## 変更カテゴリ
- Added: 新機能
- Changed: 既存機能の変更
- Deprecated: 将来削除予定の機能
- Removed: 削除された機能
- Fixed: バグ修正
- Security: セキュリティ関連の修正
-->
